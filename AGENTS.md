# ForestFireAI 开发规范

## 项目定位与适用范围

- 项目名称：ForestFireAI。
- 中文名称：基于AI和无人机的森林防火智能监测预警与决策支持系统。
- 本文件适用于整个仓库。执行任务时遵循用户明确指定的范围与约束。
- 项目来自 vue3-element-admin，当前仓库以管理后台前端为主。保留可复用基础能力，逐步建设森林防火业务。
- 长期技术方向不代表已经实现，也不构成自动安装依赖、创建后端、替换通信方式或重构现有代码的授权。

## 长期技术方向

| 领域 | 技术方向 |
| --- | --- |
| Frontend | Vue 3、TypeScript、Vite、Element Plus、Pinia、Leaflet |
| Backend | Python、FastAPI、PostgreSQL、PostGIS、Redis、MinIO |
| Realtime | WebSocket、MQTT |
| UAV | MAVLink、MAVSDK |
| AI | PyTorch、YOLO、ONNX、TensorRT |
| GIS | Leaflet、GeoServer、GDAL、Rasterio |
| Deployment | Docker、Nginx |

## 当前架构与入口

修改前必须读取相关文件，以实际实现为准，不依据模板名称或本文件推断功能已存在。

- `src/main.ts`：应用入口，注册指令、国际化、路由、Pinia、图标、权限守卫和 SSE。
- `src/App.vue`：Element Plus 全局配置、水印和顶层路由出口。
- `src/views/`：页面，目前包含 dashboard、system、profile、login、error、demo、codegen。
- `src/components/`：通用组件，包括图表、上传、字典、菜单搜索、通知和 CURD 封装。
- `src/composables/`：可复用逻辑，目前包括分页、表格选择和 SSE 服务。
- `src/api/`：按业务域组织 API，通常使用 `index.ts` 和 `types.ts`。
- `src/utils/request.ts`：Axios 请求封装，处理 Token、统一响应、错误、刷新和重试。
- `src/router/index.ts`：Hash 路由和静态入口。
- `src/router/guards/permission.ts`：登录校验、用户信息加载、动态路由注册。
- `src/stores/permission.ts`：请求 `/api/v1/menus/routes`，转换后端组件路径并生成路由；菜单消费同一份路由树。
- `src/stores/`：user、permission、app、settings、tags-view、dict、tenant 等 Pinia Store。
- `src/directives/permission/index.ts`、`src/utils/auth.ts`：按钮/角色权限判断与认证工具；实际全局注册情况查看 `src/directives/index.ts`。
- `src/layouts/`：Left、Top、Mix、Double 四种布局，以及侧栏、工具栏、页签和缓存出口。
- `src/styles/`、`src/settings.ts`：公共样式、主题变量和应用默认设置。
- `mock/`：本地 Mock；是否启用由环境配置决定。
- `types/`：路由、环境、自动导入和组件类型声明。Vite 当前关闭自动生成部分声明，新增组件时应核对类型和导入方式。

当前已有 Axios、ECharts、VueUse、UnoCSS、国际化和 SSE。Leaflet 与长期规划中的后端、无人机、AI、遥感服务尚未建立，接入时应先分析边界。当前环境可能连接上游演示 API，联调前必须核对目标地址，不向演示服务写入项目数据。

## 必须遵守的开发规则

1. 修改代码前必须先分析现有实现，包括调用链、相关类型、接口、权限、样式和测试。
2. 不允许无关重构，不顺手修改任务范围外的命名、格式、依赖或目录结构。
3. 一个任务只完成一个主要功能；为其提供必要的类型、接口、权限和测试可以属于同一任务。
4. 不删除已有正常功能，除非用户明确要求。演示功能也不能未经要求直接删除。
5. 新功能必须模块化，按页面、组件、状态、API 和业务逻辑划分职责。
6. Leaflet 逻辑不能全部写在一个 Vue 页面，必须拆分地图容器、图层和交互逻辑。
7. 后端 API 统一使用 `/api/v1` 前缀。
8. 数据库修改必须可迁移，提交版本化迁移，并说明数据兼容、回滚或恢复方式。
9. 新增核心功能必须有测试，覆盖关键业务规则、异常路径和权限边界。
10. 修改后必须运行相关测试，并如实报告执行结果；不能把类型检查当作业务测试。
11. 不自动 git commit；只有用户明确要求提交时才执行。
12. 不自动 git push；只有用户明确要求推送时才执行。
13. 无人机控制类操作必须经过人工确认。
14. LLM 只做决策支持，不直接执行危险操作。
15. 完成任务后必须报告修改文件、实现内容、测试结果和剩余问题。

## 前端模块约定

- 优先沿用 Vue 单文件组件、`<script setup lang="ts">`、Element Plus 和现有样式约定。
- 页面放在 `src/views/<业务域>/`；业务专用组件和 composable 可放在该业务目录，共享能力放在 `src/components/`、`src/composables/`。
- API 放在 `src/api/<业务域>/`，请求及响应有明确类型，复用 `src/api/common.ts` 的分页和响应约定。
- 跨页面状态放在 `src/stores/`；局部表单、弹窗和临时状态保留在页面或 composable。
- 新增业务可使用 `gis`、`uav`、`fire-event`、`ai`、`remote-sensing` 目录；仅在任务需要时创建。
- Pinia 保存必要的业务状态和共享选择，不保存 Leaflet、播放器、网络连接实例或无上限增长的轨迹、影像数据。
- 新增业务菜单沿用后端动态路由机制。组件路径必须匹配 `src/views`，路由名称保持唯一，核对隐藏页、菜单激活、页签和缓存行为。
- 保留现有认证和权限链路。前端按钮显隐不能替代后端鉴权；新增操作使用明确、统一的权限标识。
- 复用分页、字典、图表、上传和主题能力前，先确认组件实际支持范围，不为复用强行扩大封装。

## GIS、实时通信与业务边界

- GIS 是共享空间展示能力，火情、无人机、AI 结果和遥感产品通过明确的数据接口关联。
- Leaflet 的初始化、销毁、图层管理、事件订阅和尺寸适配放在组件或 composable 中；处理侧栏变化、页面缓存激活和卸载。
- 明确坐标系、经纬度顺序、距离/面积单位和时间标准，在数据边界转换，禁止隐式混用坐标。
- 地图数据按视野、时间或分页加载；大体积遥感影像使用适合的影像/切片服务，不默认整幅载入浏览器。
- 后端承担 FastAPI 业务服务、PostGIS 空间查询、Redis 缓存和 MinIO 文件存储；GDAL、Rasterio 与模型推理等计算放在服务端或任务进程。
- WebSocket、MQTT 接入需管理连接生命周期、鉴权、重连、消息去重和过期状态。当前 SSE 正常功能应继续保留，除非任务明确要求迁移。
- 页面卸载、退出登录或切换上下文时释放订阅和资源，不产生重复连接或跨用户数据残留。
- AI 检测结果、遥感异常和正式火情事件应有清晰区分，保留来源、时间、模型版本、置信度和人工复核记录。

## API 与数据库约定

- 应用业务 API 使用 `/api/v1`；`VITE_APP_BASE_API` 是代理前缀，不能与 API 版本前缀混淆或重复拼接。
- 前端普通业务 HTTP 请求复用 `src/utils/request.ts`。流媒体、地图切片和实时连接按各自协议封装。
- 当前 JSON 响应约定为 `{ code, data, msg }`，成功码为 `"00000"`；分页为 `{ list, total }`。新后端接入需保持兼容，变更契约必须同步调用方和测试。
- 数据库表、字段、索引及 PostGIS 空间结构的变化必须提供版本化迁移，不能仅手工修改数据库。
- 迁移应验证现有数据兼容性，记录不可逆影响；针对生产数据的迁移执行需有明确授权。

## 无人机与 LLM 操作约束

- MAVLink、MAVSDK 控制由专门服务封装，前端不直接持有设备控制凭据。
- 解锁、起飞、降落、返航、航线下发、任务执行、模式切换等控制操作在实际下发前必须由有权限的人员确认。
- 确认界面必须明确目标设备、操作内容和关键参数；后端校验权限与确认状态，并记录操作者、请求及执行结果。
- LLM 可以解释告警、生成建议和草拟任务，但不能绕过人工确认直接触发无人机控制或其他危险操作。
- 测试优先使用 Mock、仿真或 SITL；不得在普通测试中向真实设备发送控制命令。

## 验证与交付

- 使用项目现有 pnpm 工具链，先查看 `package.json`，不要假设测试脚本存在。
- 当前提供 `pnpm run type-check` 和 `pnpm run build`，没有独立业务测试脚本。新增核心功能时必须提供可执行测试；如缺少必要测试环境，明确报告原因和未验证范围，不能声称通过。
- 修改后运行与变更相关的测试；前端代码变更运行类型检查，涉及打包、路由或依赖时增加构建验证。后端和数据库建立后运行相应业务、接口与迁移测试。
- 仅文档变更时执行内容、路径与格式检查，无需运行无关业务测试。
- 当前 lint 脚本带有 `--fix` 或 `--write`，会修改文件。避免用全项目自动修复作为只读验证，防止引入无关变更。
- 不擅自升级依赖、安装整套规划技术栈或运行部署脚本。用户禁止安装依赖、Git 操作或指定文件外修改时必须遵守。
- 保留用户已有变更，发现任务范围外问题只记录，不自动修复。

任务完成后按以下内容报告，即使没有剩余问题也明确写出：

- 修改文件：列出新增、修改、删除的文件。
- 实现内容：说明完成的功能及行为变化。
- 测试结果：列出实际执行的测试或检查、结果，以及未执行项的原因。
- 剩余问题：说明已知问题、限制、依赖或待办；无则写“无”。
