import { DevframeClientHost, DevframeClientHostOptions, DevframeClientRuntime, DevframeClientRuntime as DevframeClientRuntime$1, DevframeClientRuntimeOptions, DevframeClientRuntimeOptions as DevframeClientRuntimeOptions$1 } from "@devframes/hub/client";
//#region src/hub-client.d.ts
interface MountDevframeHubClientOptions extends DevframeClientRuntimeOptions$1 {
  /**
   * Hub mount base to connect to. Forwarded as `connect.baseURL` when no
   * `rpc` / `connect.baseURL` is supplied.
   *
   * @default '/__devframes/'
   */
  base?: string;
}
/**
 * Boot the devframes-hub **client runtime** in the host page: the browser
 * half of {@link import('./hub').viteDevframeHub}. Connects RPC to the hub
 * (defaulting `base` to `/__devframes/`), assembles the shared
 * `DevframeClientContext`, and imports each dock's client script into the
 * page. A thin default-applying wrapper over `@devframes/hub/client`'s
 * `createDevframeClientRuntime`.
 *
 * Only needed when you render your own dock UI (or override the hub's `ui`).
 * With the default `@devframes/hub-ui`, its injected `embedded.js` boots the
 * client for you, so the host page needs no client code.
 */
declare function mountDevframeHubClient(options?: MountDevframeHubClientOptions): Promise<DevframeClientRuntime$1>;
//#endregion
export { type DevframeClientHost, type DevframeClientHostOptions, type DevframeClientRuntime, type DevframeClientRuntimeOptions, MountDevframeHubClientOptions, mountDevframeHubClient };