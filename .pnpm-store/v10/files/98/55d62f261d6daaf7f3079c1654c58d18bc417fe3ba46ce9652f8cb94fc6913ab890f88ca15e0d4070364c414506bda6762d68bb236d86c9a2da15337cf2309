import { t as createDefineWrapperWithContext } from "./define-BLWPsH6y.mjs";
import { t as resolveClientAssets$1 } from "./client-assets-DNdhagGm.mjs";
//#region src/define.ts
const defineRpcFunction = createDefineWrapperWithContext();
/**
* Identity helper that types a devframe definition, the primary authoring
* entry point. Import it from the package root: `import { defineDevframe } from 'devframe'`.
*/
function defineDevframe(d) {
	return d;
}
/**
* @deprecated Read {@link DevframeDefinition.clientAssets} from the definition
* directly. This helper is internal machinery that was never meant to be part
* of the public API and is scheduled for removal.
*/
function resolveClientAssets(d) {
	return resolveClientAssets$1(d);
}
//#endregion
export { defineDevframe, defineRpcFunction, resolveClientAssets };
